export const counter = () => {
    return ({
        type: 'INCREMENT',
        // payload: { 
        //     profile: data.profileObj,
        //     accessToken: data.accessToken,
        // },
    })
}